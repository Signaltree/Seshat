# Auditoría Completa — Seshat — 2026-07-17

## Resumen Ejecutivo

- **39 archivos Java (~2267 LOC) + 15 templates HTML (~1210 LOC) + 0 tests**
- App funcional, bien estructurada, con seguridad básica sólida
- **0 vulnerabilidades críticas explotables** — el código más riesgoso está mitigado
- **6 hallazgos 🔴 críticos** de integridad de datos y consistencia transaccional
- **3 hallazgos 🟡 altos** de seguridad para producción

---

## 🔴 Hallazgos Prioritarios (antes de producción)

| # | Severidad | Hallazgo | Archivo | Acción |
|---|-----------|----------|---------|--------|
| 1 | 🔴 | `PersonaService.eliminar()` elimina archivos físicos DENTRO de la transacción → si rollback, archivos perdidos | PersonaService.java:61-78 | Mover file I/O a `afterCommit()` callback |
| 2 | 🔴 | `PadrinoService.agregar()` sin `@Transactional` → si falla join insert, padrino huérfano | PadrinoService.java:30-49 | Agregar `@Transactional` |
| 3 | 🔴 | `BautizoService.eliminar()`, `ConfirmacionService.eliminar()`, `MatrimonioService.eliminar()` sin `@Transactional` | 3 services | Agregar `@Transactional` |
| 4 | 🔴 | `FotoController.guardar()` — `LocalDate.parse()` FUERA del bloque try-catch → error 500 si fecha inválida | FotoController.java:37 | Mover parse DENTRO del try |
| 5 | 🔴 | Listados de sacramentos muestran `persona_id` numérico en vez del nombre de la persona | 3 listar.html | Hacer JOIN en SQL + agregar campo nombre |
| 6 | 🔴 | `CertificadoService.eliminar()` elimina archivo físico ANTES del DELETE SQL → archivo perdido si falla SQL | CertificadoService.java:49-53 | Reordenar: eliminar BD primero |

## 🟡 Hallazgos de Seguridad

| # | Severidad | Hallazgo | Solución |
|---|-----------|----------|----------|
| 7 | 🟡 | Cookie de sesión sin `secure=true` (CWE-614) | `server.servlet.session.cookie.secure=true` |
| 8 | 🟡 | RateLimitingFilter ignora `X-Forwarded-For` (CWE-307) | Agregar `obtenerIpCliente()` con header |
| 9 | 🟡 | Sin SRI integrity hashes en CDNs (CWE-829) | Agregar `integrity` a todos los CDN scripts |
| 10 | 🟡 | `model/*.java` sin `equals()/hashCode()` en ningún modelo | Implementar en los 7 POJOs |
| 11 | 🟡 | Sacramentos sin validación de entrada (Bautizo, Confirmacion, Matrimonio) | Agregar `validar()` en services |
| 12 | 🟡 | `queryForObject` sin manejo de `EmptyResultDataAccessException` en 7 repos | Usar `query() + stream().findFirst()` |
| 13 | 🟡 | Inconsistencia naming snake_case vs camelCase entre modelos | Unificar a camelCase |
| 14 | 🟡 | GlobalExceptionHandler no distingue códigos HTTP (siempre 200) | Usar `@ResponseStatus` |
| 15 | 🟡 | Logout form sin CSRF token explícito (funciona por HTMX pero by design frágil) | Agregar `<input type="hidden" name="_csrf">` |
| 16 | 🟡 | `RateLimitingFilter` — off-by-one: 4 intentos en vez de 5 | `new AtomicInteger(0)` no `1` |

## ⚪ Mejoras Arquitectónicas

| # | Hallazgo | Archivo |
|---|----------|---------|
| 17 | Duplicación masiva CRUD: 3 servicios/controllers/repos casi idénticos | Bautizo/Confirmacion/Matrimonio |
| 18 | Join tables sin PK compuesta → duplicados potenciales | schema.sql |
| 19 | Sin índices en FK columns (7+) | schema.sql |
| 20 | Dashboard `contar()` ignora `mes` si `anio` es null | DashboardService.java |
| 21 | MIME type inferido solo por extensión, no por magic bytes reales | CertificadoService.java, FotoService.java |
| 22 | error.html usa paleta antigua (granate/cuero) en vez de nueva (forest/gold) | error.html |
| 23 | Schema.sql usa TEXT para campos con longitud conocida | schema.sql |
| 24 | `@RequestParam id` redundante en controllers | PersonaController.java |
| 25 | FotoController sin `Cache-Control` headers | FotoController.java |
| 26 | Import `java.sql.Statement` no usado en 5 repos | Repositories |
| 27 | `ValidacionUtil.RUT` regex inactiva (código muerto) | ValidacionUtil.java |
| 28 | RUT validation rechaza cuerpos < 7 dígitos | ValidacionUtil.java |
| 29 | `List<Map<String, Object>>` en DTO sin type safety | StatsDashboard.java |

## 🔧 Stack de Producción Recomendado

```
Seshat Production Stack (costo: $0)
├── Docker multi-stage (eclipse-temurin:21-jre-alpine)
├── docker-compose: app + postgres:16 + nginx
├── nginx reverse proxy + Let's Encrypt HTTPS
├── pg_dump backups diarios (30 días retención)
├── JUnit 5 + Testcontainers + Mockito (tests)
├── GitHub Actions: compile → test → OWASP → build
├── Spring Boot Actuator (/health, /metrics)
└── Logback con rotación (10MB, 30 días)
```

## 🧪 Plan de Testing

| Prioridad | Test | Técnica |
|-----------|------|---------|
| P1 | `ValidacionUtilTest` | Unit (pure logic) |
| P2 | `FileStorageServiceTest` | Unit (temp dirs) |
| P3 | `PersonaServiceTest` | Unit (Mockito) |
| P4 | `PadrinoServiceTest` | Unit (Mockito) |
| P5 | `PersonaRepositoryTest` | Integration (Testcontainers) |
| P6 | `SecurityConfigTest` | Integration (MockMvc) |
| P7 | `RateLimitingFilterTest` | Integration (MockMvc) |
| P8 | `DashboardServiceTest` | Integration (Testcontainers) |
| P9 | `BautizoServiceTest` | Unit (Mockito) |
| P10 | `PersonaControllerTest` | Integration (MockMvc) |

## Dependencias a agregar al pom.xml

```xml
spring-boot-starter-test (test)
testcontainers (1.19.8, test)
junit-jupiter (1.19.8, test)
postgresql (1.19.8, test)
spring-boot-starter-actuator (opcional)
```

## Skills instalados en esta sesión

| Skill | Installs | Propósito |
|-------|----------|-----------|
| find-skills | 2.5M | Meta-skill de descubrimiento |
| java-springboot | 18K | Patrones Spring Boot |
| java-junit | 10.8K | Testing Java |
| review-and-refactor | 10.7K | Code review |
| postgresql-code-review | 11.2K | Revisión PostgreSQL |
| documentation-and-adrs | 13.1K | Documentación |
| maven-dependency-audit | 104 | Auditoría dependencias |
